// ? to Add  a random color to the card : 
// *  Colors.primaries[Random().nextInt(Colors.primaries.length)] 
//------------------------------------------------------------------
// ? what is the different between hintText and labelText ?? 
// * the different is in hintText the Text not hidden in virus modes (active , focus) , 
// * in labelText it just a label and it hidden in focus mode 
//------------------------------------------------------------------
/*  in maxline to create a parameter get the maxlines 
 you should create a final varialbe and give the value 1 to 
 the maxline variable in the constructor */ 
 // * final String maxLines ; 
 //* const customTextField ({super.key , maxlines = 1 })  ; 
 //----------------------------------------------------------------
 